org.gradle.jvmargs=-Xmx1200m -XX:MaxMetaspaceSize=600m -XX:ReservedCodeCacheSize=128m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disabled: Kotlin's incremental compiler cache fails on Windows when the
# project and the Gradle/pub caches live on different drive letters
# ("this and base files have different roots").
kotlin.incremental=false
