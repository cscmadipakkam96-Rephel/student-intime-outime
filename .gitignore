# Server (Node/Express)
server/node_modules/
server/.env
server/generated/

# Firebase service account key — secret credential, never commit
server/firebase-service-account.json

# Database backups — contain real student PII, never commit
backups/

# AI tool configs (not part of the app)
.claude/
.agents/
.windsurf/
skills-lock.json

# Client (Flutter) build output and JVM crash dumps from Gradle OOM kills
client/build/
client/android/build/
client/android/app/build/
hs_err_pid*.log
replay_pid*.log
